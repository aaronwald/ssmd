---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: connectors.ssmd.ssmd.io
spec:
  group: ssmd.ssmd.io
  names:
    kind: Connector
    listKind: ConnectorList
    plural: connectors
    singular: connector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.feed
      name: Feed
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.messagesPublished
      name: Messages
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connector is the Schema for the connectors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Connector
            properties:
              categories:
                description: Categories filters to specific event categories (optional,
                  empty = all)
                items:
                  type: string
                type: array
              cdc:
                description: Cdc configures CDC-driven dynamic market subscriptions
                properties:
                  consumerName:
                    description: |-
                      ConsumerName is the durable consumer name (unique per connector instance)
                      Defaults to "{connector-name}-cdc"
                    type: string
                  enabled:
                    default: false
                    description: |-
                      Enabled enables CDC-driven dynamic market subscriptions
                      When enabled, the connector will subscribe to new markets automatically
                      when they are inserted into secmaster
                    type: boolean
                  streamName:
                    default: SECMASTER_CDC
                    description: StreamName is the NATS JetStream stream for CDC events
                    type: string
                type: object
              closeWithinHours:
                description: |-
                  CloseWithinHours only subscribes to markets closing within this many hours
                  Useful for high-volume categories like Sports where only near-term markets are relevant
                  Requires daily connector restart to pick up newly eligible markets
                format: int32
                type: integer
              date:
                description: Date is the trading date (optional, used for historical
                  backfill)
                type: string
              excludeCategories:
                description: ExcludeCategories excludes specific categories (for sharding)
                items:
                  type: string
                type: array
              feed:
                description: Feed references the feed ConfigMap (e.g., "kalshi" →
                  feed-kalshi ConfigMap)
                type: string
              image:
                description: Image is the container image to use (optional, defaults
                  from feed ConfigMap)
                type: string
              replicas:
                default: 1
                description: Replicas is the number of connector pods (optional, defaults
                  to 1)
                format: int32
                type: integer
              resources:
                description: Resources configures CPU/memory for the connector pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              secretRef:
                description: SecretRef references the credentials secret
                properties:
                  apiKeyField:
                    default: api-key
                    description: APIKeyField is the key in the secret containing the
                      API key
                    type: string
                  name:
                    description: Name is the secret name
                    type: string
                  privateKeyField:
                    default: private-key
                    description: PrivateKeyField is the key in the secret containing
                      the private key
                    type: string
                required:
                - name
                type: object
              transport:
                description: Transport configures the NATS connection
                properties:
                  stream:
                    description: Stream is the JetStream stream name
                    type: string
                  subjectPrefix:
                    description: SubjectPrefix is the NATS subject prefix (e.g., "prod.kalshi")
                    type: string
                  type:
                    default: nats
                    description: Type is the transport type (currently only "nats"
                      supported)
                    type: string
                  url:
                    description: URL is the NATS server URL
                    type: string
                type: object
            required:
            - feed
            type: object
          status:
            description: status defines the observed state of Connector
            properties:
              conditions:
                description: Conditions represent the current state of the Connector
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectionState:
                description: ConnectionState is the WebSocket connection state
                enum:
                - connected
                - reconnecting
                - disconnected
                type: string
              deployment:
                description: Deployment is the name of the created Deployment
                type: string
              lastMessageAt:
                description: LastMessageAt is the timestamp of the last message
                format: date-time
                type: string
              messagesPublished:
                description: MessagesPublished is the total messages published to
                  NATS
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - Starting
                - Running
                - Failed
                - Terminated
                type: string
              startedAt:
                description: StartedAt is when the connector started
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
