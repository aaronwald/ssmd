---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: signals.ssmd.ssmd.io
spec:
  group: ssmd.ssmd.io
  names:
    kind: Signal
    listKind: SignalList
    plural: signals
    singular: signal
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.signals
      name: Signals
      type: string
    - jsonPath: .spec.source.stream
      name: Stream
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Signal is the Schema for the signals API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Signal
            properties:
              image:
                description: Image is the container image to use
                type: string
              outputPrefix:
                default: signals
                description: |-
                  OutputPrefix is the NATS subject prefix for signal fires (e.g., "signals")
                  Fires are published to: {outputPrefix}.{signal-id}.fires
                type: string
              resources:
                description: Resources configures CPU/memory for the signal pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              signals:
                description: Signals is the list of signal IDs to run in this pod
                items:
                  type: string
                minItems: 1
                type: array
              source:
                description: Source configures the NATS source for market data
                properties:
                  categories:
                    description: Categories filters to specific event categories (optional,
                      empty = all)
                    items:
                      type: string
                    type: array
                  filter:
                    description: |-
                      Filter is the NATS subject filter pattern (e.g., "prod.kalshi.politics.>")
                      Optional - if not specified, defaults to "prod.kalshi.>"
                    type: string
                  natsUrl:
                    description: NATSURL is the NATS server URL (optional, defaults
                      to cluster NATS)
                    type: string
                  stream:
                    description: Stream is the JetStream stream name (e.g., "PROD_KALSHI")
                    type: string
                  tickers:
                    description: Tickers filters to specific tickers (optional, wildcards
                      supported like "PRES*")
                    items:
                      type: string
                    type: array
                required:
                - stream
                type: object
            required:
            - image
            - signals
            - source
            type: object
          status:
            description: status defines the observed state of Signal
            properties:
              conditions:
                description: Conditions represent the current state of the Signal
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployment:
                description: Deployment is the name of the created Deployment
                type: string
              phase:
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - Running
                - Failed
                type: string
              signalMetrics:
                description: SignalMetrics contains per-signal metrics
                items:
                  description: SignalMetrics contains per-signal metrics
                  properties:
                    firesEmitted:
                      description: FiresEmitted is the total fires emitted by this
                        signal
                      format: int32
                      type: integer
                    lastFireAt:
                      description: LastFireAt is the timestamp of the last fire
                      format: date-time
                      type: string
                    messagesProcessed:
                      description: MessagesProcessed is the total messages processed
                        by this signal
                      format: int64
                      type: integer
                    signal:
                      description: Signal is the signal ID
                      type: string
                  required:
                  - signal
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
