---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: notifiers.ssmd.ssmd.io
spec:
  group: ssmd.ssmd.io
  names:
    kind: Notifier
    listKind: NotifierList
    plural: notifiers
    singular: notifier
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.destinations
      name: Destinations
      priority: 1
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.firesReceived
      name: Fires
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Notifier is the Schema for the notifiers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Notifier
            properties:
              destinations:
                description: Destinations configures notification destinations with
                  routing rules
                items:
                  description: NotifierDestination defines a notification destination
                    with routing rules
                  properties:
                    config:
                      description: Config contains destination-specific configuration
                      properties:
                        channel:
                          description: Channel is the channel name (for Slack/Discord)
                          type: string
                        from:
                          description: From is the sender email address (for email)
                          type: string
                        priority:
                          description: 'Priority is the notification priority (for
                            ntfy: min, low, default, high, urgent)'
                          type: string
                        server:
                          description: Server is the server URL (for ntfy)
                          type: string
                        smtp:
                          description: SMTP is the SMTP server configuration (for
                            email)
                          type: string
                        to:
                          description: To is the recipient email address (for email)
                          type: string
                        topic:
                          description: Topic is the topic name (for ntfy)
                          type: string
                        webhookUrl:
                          description: WebhookURL is the webhook URL (for Slack/Discord)
                          type: string
                      type: object
                    match:
                      description: Match defines routing rules for this destination
                        (optional, empty = all fires)
                      properties:
                        field:
                          description: Field is the JSON path to the field to match
                            (e.g., "payload.dollarVolume")
                          type: string
                        operator:
                          description: Operator is the comparison operator
                          enum:
                          - eq
                          - ne
                          - gt
                          - gte
                          - lt
                          - lte
                          - contains
                          - regex
                          type: string
                        value:
                          description: Value is the value to compare against
                          type: string
                      required:
                      - field
                      - operator
                      - value
                      type: object
                    name:
                      description: Name is the unique name for this destination
                      type: string
                    secretRef:
                      description: SecretRef references a secret containing credentials
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    template:
                      description: Template is the notification message template
                      type: string
                    type:
                      description: Type is the destination type (ntfy, slack, discord,
                        email)
                      enum:
                      - ntfy
                      - slack
                      - discord
                      - email
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              image:
                description: Image is the container image to use
                type: string
              resources:
                description: Resources configures CPU/memory for the notifier pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              source:
                description: Source configures what NATS subjects to subscribe to
                properties:
                  natsUrl:
                    description: NATSURL is the NATS server URL (optional, defaults
                      to cluster NATS)
                    type: string
                  subjects:
                    description: Subjects is the list of NATS subjects to subscribe
                      to (wildcards supported)
                    items:
                      type: string
                    minItems: 1
                    type: array
                required:
                - subjects
                type: object
            required:
            - destinations
            - image
            - source
            type: object
          status:
            description: status defines the observed state of Notifier
            properties:
              conditions:
                description: Conditions represent the current state of the Notifier
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployment:
                description: Deployment is the name of the created Deployment
                type: string
              destinationMetrics:
                description: DestinationMetrics contains per-destination metrics
                items:
                  description: DestinationMetrics contains per-destination metrics
                  properties:
                    failed:
                      description: Failed is the total notifications failed
                      format: int32
                      type: integer
                    lastError:
                      description: LastError is the last error message (if any)
                      type: string
                    lastSentAt:
                      description: LastSentAt is the timestamp of the last sent notification
                      format: date-time
                      type: string
                    name:
                      description: Name is the destination name
                      type: string
                    sent:
                      description: Sent is the total notifications sent
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
              firesReceived:
                description: FiresReceived is the total fires received from NATS
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - Running
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
