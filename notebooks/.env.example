# Copy to .env and fill in. .env is gitignored — never commit a real key.
# Your datasets:read key (manage at https://harman.varshtat.com → Admin → API Keys).
SSMD_API_KEY=sk_live_xxxxxxxx_replace_me

# Public data API base. Leave as-is unless testing another environment.
SSMD_API_BASE=https://api.varshtat.com
